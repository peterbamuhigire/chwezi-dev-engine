# Godot mobile export gates

Back to [Godot Mobile Game Development](../SKILL.md).

Pin editor and matching export templates. Verify current JDK/SDK/NDK/Xcode, Gradle/AAB, signing, plugin and store requirements from official docs. Audit addons and custom templates. Run exported release builds on the defined device matrix; editor play is not evidence.

Test permissions, input, safe areas, pause/focus/suspend, saves, offline, update path, symbols, package contents, performance and heat. Preserve export presets without secrets and promote the tested artefact.
